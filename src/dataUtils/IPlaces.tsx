export interface Places {
    name: string,
    location: string,
    rating: number,
    features: Array<string>,
    imgSource: string,
    pricing: number,
    queue: number,
    capacity: number,
}